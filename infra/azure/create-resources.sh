#!/usr/bin/env bash
set -e
echo "THIS IS A PLACEHOLDER: add az cli commands to provision RG, ACR, App Service, and deployment slots"
echo "Example: az group create --name my-rg --location westus"
echo "az acr create --resource-group my-rg --name myacr --sku Standard --location westus"
echo "az webapp create --resource-group my-rg --plan myPlan --name myapp --deployment-container-image-name myacr.azurecr.io/gas:latest"
