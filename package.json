{
  "name": "gas-project-rashida",
  "version": "0.1.0",
  "description": "GAS Project \u2014 personal implementation (local-first)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "lint": "eslint .",
    "test": "jest --runInBand",
    "build": "echo \"no build required\"",
    "docker:build": "docker build -t gas .",
    "docker:run": "docker run --rm -p 3000:3000 gas"
  },
  "dependencies": {
    "express": "^4.18.2",
    "prom-client": "^14.0.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "nodemon": "^2.0.20",
    "supertest": "^6.3.0"
  },
  "engines": {
    "node": ">=16"
  }
}