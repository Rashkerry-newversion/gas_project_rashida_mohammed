#!/usr/bin/env bash
# Example to set slot traffic - replace variables
# az webapp traffic set --distribution staging=10 production=90 --name <app-name> --resource-group <rg>
echo "Use az webapp traffic set --distribution staging=10 production=90 --name <app-name> --resource-group <rg>"
